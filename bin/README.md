# `bin/`

Helper scripts for operators.

- `setup.sh` — first-time setup (wraps `composer run setup`)
- `update.sh` — safe update checklist in one command

> On Linux/macOS you may need: `chmod +x bin/*.sh`
